import time\nimport json\nimport curses\n\ndef main(stdscr):\n    with open('texts.json') as f:\n        texts = json.load(f)\n    stdscr.clear()\n    stdscr.addstr(0, 0, "Select difficulty level:")\n    stdscr.addstr(1, 0, "1. Easy\n2. Medium\n3. Hard")\n    difficulty = stdscr.getkey()\n\n    if difficulty == '1':\n        paragraph = texts['easy'][0]\n    elif difficulty == '2':\n        paragraph = texts['medium'][0]\n    elif difficulty == '3':\n        paragraph = texts['hard'][0]\n    else:\n        stdscr.addstr(2, 0, "Invalid choice. Defaulting to easy.")\n        paragraph = texts['easy'][0]\n\n    stdscr.clear()\n    stdscr.addstr(0, 0, paragraph)\n\n    stdscr.addstr(2, 0, "Type the above text:")\n    stdscr.refresh()\n\n    start_time = time.time()\n    input_text = ""\n    while True:\n        char = stdscr.getkey()\n        input_text += char\n        if input_text != paragraph[:len(input_text)]:\n            stdscr.addstr(3, 0, "\u001b[31mIncorrect\u001b[0m")\n        else:\n            stdscr.addstr(3, 0, "Correct")\n\n        stdscr.addstr(4, 0, input_text)\n\n        if time.time() - start_time > 60:\n            break\n\n    stdscr.addstr(5, 0, "Time's up!")\n    stdscr.refresh()\n    stdscr.getkey()\n\ncurses.wrapper(main)