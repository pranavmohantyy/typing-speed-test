import time\nimport json\n\nwith open('texts.json') as f:\n    texts = json.load(f)\n\nprint("Select difficulty level:")\nprint("1. Easy\n2. Medium\n3. Hard")\ndifficulty = input("Enter the number of your choice: ")\n\nif difficulty == '1':\n    paragraph = texts['easy'][0]\nelif difficulty == '2':\n    paragraph = texts['medium'][0]\nelif difficulty == '3':\n    paragraph = texts['hard'][0]\nelse:\n    print("Invalid choice. Defaulting to easy.")\n    paragraph = texts['easy'][0]\n\nprint(paragraph)\n\nprint("Select time mode:")\nprint("1. 30 seconds\n2. 1 minute\n3. 2 minutes")\ntime_mode = input("Enter the number of your choice: ")\n\nif time_mode == '1':\n    time_limit = 30\nelif time_mode == '2':\n    time_limit = 60\nelif time_mode == '3':\n    time_limit = 120\nelse:\n    print("Invalid choice. Defaulting to 1 minute.")\n    time_limit = 60\n\ninput("Press Enter to start...")\nstart_time = time.time()\n\ntyped_text = input("Start typing: ")\nend_time = time.time()\n\nelapsed_time = end_time - start_time\nwords = paragraph.split()\ntyped_words = typed_text.split()\n\ncorrect_words = [w for w in typed_words if w in words]\naccuracy = len(correct_words) / len(words) * 100\n\nprint(f"Time taken: {elapsed_time:.2f} seconds")\nprint(f"Accuracy: {accuracy:.2f}%")\n\nword_times = {}\nfor word in words:\n    word_times[word] = 0\n\nfor word in typed_words:\n    if word in word_times:\n        word_start_time = time.time()\n        word_times[word] += word_start_time - end_time\n\nslowest_words = sorted(word_times.items(), key=lambda x: x[1], reverse=True)[:5]\n\nprint("Slowest words:")\nfor word, duration in slowest_words:\n    print(f"{word}: {duration:.2f} seconds")