import time\nimport json\n\nwith open('texts.json') as f:\n    texts = json.load(f)\n\nprint("Select difficulty level:")\nprint("1. Easy\n2. Medium\n3. Hard")\ndifficulty = input("Enter the number of your choice: ")\n\nif difficulty == '1':\n    paragraph = texts['easy'][0]\nelif difficulty == '2':\n    paragraph = texts['medium'][0]\nelif difficulty == '3':\n    paragraph = texts['hard'][0]\nelse:\n    print("Invalid choice. Defaulting to easy.")\n    paragraph = texts['easy'][0]\n\nprint(paragraph)\n\nprint("Select time mode:")\nprint("1. 30 seconds\n2. 1 minute\n3. 2 minutes")\ntime_mode = input("Enter the number of your choice: ")\n\nif time_mode == '1':\n    duration = 30\nelif time_mode == '2':\n    duration = 60\nelif time_mode == '3':\n    duration = 120\nelse:\n    print("Invalid choice. Defaulting to 1 minute.")\n    duration = 60\n\ninput("Press Enter to start...")\nstart_time = time.time()\n\nprint("Type the following:")\nprint(paragraph)\n\nuser_input = input("\n")\nend_time = time.time()\n\nelapsed_time = end_time - start_time\nwords_typed = len(user_input.split())\nminutes_taken = elapsed_time / 60\nWPM = words_typed / minutes_taken\n\naccuracy = (sum(1 for i, char in enumerate(user_input) if char == paragraph[i]) / len(paragraph)) * 100\n\nresults = {\n    'timestamp': time.strftime('%Y-%m-%d %H:%M:%S'),\n    'WPM': WPM,\n    'accuracy': accuracy,\n    'difficulty': difficulty\n}\n\nwith open('results.json', 'a') as result_file:\n    json.dump(results, result_file)\n    result_file.write('\n')\n\nprint(f'Your WPM: {WPM:.2f}, Accuracy: {accuracy:.2f}%, Difficulty: {difficulty}')